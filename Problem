这里有个问题需要记录

在main函数中，先后声明了一个数组和一个Node结构。当数组的大小大于126时，Node中的next指向的地址不为NULL；i
当数组大小小于等于126时,Node中的next指向的地址为null。我认为原因在于编译器在为局部变量分配地址的时候是随机的
，这就导致分配到的地址可能与预期不一样。因此我们需要对任何分配的地址进行初始化。

在刚开始的时候，dlist_init没有能初始化Node结构，原因在于，虽然我将Node的地址传给了他，但是又给他分动态分配
了一个空间，导致函数中的指针并为指向Node地址。从而也未初始化成功。
